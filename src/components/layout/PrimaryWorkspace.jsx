/**
 * KodNest Premium Build System — Primary Workspace
 * 70% width. Main product interaction. Clean cards, predictable components.
 */

export function PrimaryWorkspace({ children }) {
  return (
    <main className="kn-workspace" role="main">
      {children}
    </main>
  );
}
